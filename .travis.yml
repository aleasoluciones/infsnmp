sudo: required

services:
- docker

language: python

python:
- '2.7'

# safelist branch to build
branches:
  only:
      - master

before_install:
- sudo apt-get -y update || true

install:
- dev/setup_venv.sh

script:
- dev/all_tests.sh

notifications:
  email:
    - bifer@alea-soluciones.com
